import * as React from 'react';

interface IBillListProps {
}

const BillList: React.FunctionComponent<IBillListProps> = (props) => {
  return (
    <div>
        Here go the bill List!
    </div>
  );
};

export default BillList;

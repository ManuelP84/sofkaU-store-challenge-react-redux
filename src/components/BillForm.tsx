import * as React from 'react';

interface IBillFormProps {
}

const BillForm: React.FunctionComponent<IBillFormProps> = (props) => {
  return (
    <div>
        Here go the Bill form!
    </div>
  );
};

export default BillForm;

import * as React from 'react';

interface IBillProps {
}

const Bill: React.FunctionComponent<IBillProps> = (props) => {
  return (
    <div>
        Here go the bill!
    </div>
  );
};

export default Bill;
